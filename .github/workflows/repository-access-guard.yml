name: Repository Access Guard

on:
  pull_request:
    types: [opened, edited, synchronize, reopened]
  push:
    branches: ['**']
  workflow_dispatch:

jobs:
  guard:
    runs-on: ubuntu-latest
    steps:
      - name: Access guard relaxed
        run: |
          echo "No actor/branch restriction enforced by this workflow."
